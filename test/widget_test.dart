// 占位测试。原文件是 Flutter 模板自带的 Counter 测试，但 MyApp 没有 Counter 会直接失败，故替换为占位。

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder: 测试框架可用', () {
    expect(1 + 1, 2);
  });
}
