gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Gradle build output. Use the leading slash so it anchors at android/ (this
# file's directory) and matches everything under android/build/, not just the
# directory entry — without it, files added explicitly via `git add` slip past
# the rule (the build/ dir is matched but its contents are not).
/build/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
